spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.68.10:8848
      config:
        server-addr: 192.168.68.10:8848
        file-extension: yml
        prefix: ${spring.application.name}
server:
  port: 8085